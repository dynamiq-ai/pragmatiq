"""Contract tests for pragmatiq's public API.

These tests pin the frozen surface (function signatures, CLI command paths and
parameter names, model API) so that internal restructuring cannot silently break
the surface that downstream consumers depend on.

See ``docs/STABILITY.md`` for the full stability contract.
"""
